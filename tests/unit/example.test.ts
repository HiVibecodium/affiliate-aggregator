describe('Affiliate Aggregator', () => {
  it('should pass initial test', () => {
    expect(true).toBe(true)
  })

  it('should have correct project name', () => {
    const projectName = 'affiliate-aggregator'
    expect(projectName).toBe('affiliate-aggregator')
  })
})
